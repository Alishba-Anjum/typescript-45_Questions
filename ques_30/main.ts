let userNames: string[] = ["admin", "alishba", "samiya", "areesha", "habiba"];
userNames.forEach(userNames => {
    if (userNames == "admin") {
        console.log("hello admin!");   
    }else{
        console.log(`Hello ${userNames}`);
        
    }
    
});